{
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "scripts": {
    "dev": "concurrently \"npm run dev --prefix api\" \"npm run dev --prefix frontend\""
  },
  "name": "PFC",
  "private": true,
  "workspaces": [
    "frontend",
    "api"
  ],
  "dependencies": {
    "@sendgrid/mail": "^8.1.6"
  }
}
